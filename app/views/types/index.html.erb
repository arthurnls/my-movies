<h2>Types of Netflix Titles</h2>
<p>List of all Types of Netflix Titles</p>

<% if @types.empty? %>
    <p>There are no types registered.</p>
<% else %>
    <ul class="list-group list-inline">
        <% @types.each do |type| %>
            <li class="list-group-item list-inline-item"><%= link_to type.name, type %> (Total: <%= type.netflix_titles.count %>)</li>
        <% end %>
    </ul>
<% end %>
