<h2>Netflix Titles</h2>
<p>Netflix Titles has a total of:</p>
<ul>
    <li>Genres: <%= @total_genres %></li>
    <li>Actors: <%= @total_actors %></li>
    <li>Directors: <%= @total_directors %></li>
</ul>
<p>A total of <%= @total_users %> users have watched at least one Netflix Title.</p>
<p>List of all Netflix Titles in My Movies Web App database</p>
<small><a href="#type1">Jump to <%= @types[1].name %>s</a></small>
<ul class="list-group list-inline">
    <li>
        <hr/><h4>&nbsp&nbsp&nbsp<%= @types[0].name %>s</h4>
        <ul class="list-group list-inline">
        <% if @netflix_titles_type0.empty? %>
            <li>There are no Netflix <%= @types[0].name %>s registered.</li>
        <% else %>
            <% @netflix_titles_type0.each do |netflix_title| %>
            <li class="list-group-item list-inline-item"><%= link_to netflix_title.title, netflix_title %></li>
            <% end %>
        </ul>
        <% end %>
    </li>

    <li class="mt-3">
        <span id="type1" style="position:relative; top: -50px; display:hidden;" ></span>
        <hr/><h4>&nbsp&nbsp&nbsp<%= @types[1].name %>s</h4>
        <ul class="list-group list-inline">
        <% if @netflix_titles_type1.empty? %>
            <li>There are no Netflix <%= @types[1].name %>s registered.</li>
        <% else %>
            <% @netflix_titles_type1.each do |netflix_title| %>
            <li class="list-group-item list-inline-item"><%= link_to netflix_title.title, netflix_title %></li>
            <% end %>
        </ul>
        <% end %>
    </li>
</ul>
